- meta title: "Lorenzo Sinisi - Posts", description: "Articles by Lorenzo Sinisi - Ruby Developer", keywords: %w(ruby fullstack rails)
%h1 Archive:

- @posts.each do |post|
  %content
    %h2= post.title
    .content= simple_format(post.content)
    %span= link_to 'Read more >>', post
  %hr
